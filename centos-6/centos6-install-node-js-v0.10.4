Instaling Node.js

1.  Melihat group install "Development Tools"
    # yum -y groupinstall "Development Tools"
    or
    # yum install gcc-c++ make
2.  Install screen 
    # yum -y install screen
3.  Download url
    # curl --silent --location https://rpm.nodesource.com/setup_6.x | bash -
4.  Install 
    # yum -y install nodejs
5.  Untuk melihat versi node.js
    # node --version
--------------------------------------------------------
    Jika masih bermasalah maka bisa menggunakan tutorial
    # https://nodejs.org/en/download/package-manager/

Selesai, silahkan cek node.js sudah running atau belum.
Create by : lani.asep@lawencon.com 
